# Copy this into Streamlit Community Cloud's app settings -> Secrets
# (not into a committed secrets.toml -- .gitignore already excludes that file).
#
# SUPABASE_URL is the project's public API URL, not sensitive on its own.
# SUPABASE_SERVICE_ROLE_KEY bypasses RLS -- treat it like a password.
# Find both in the Supabase dashboard: Project Settings -> API.

SUPABASE_URL = "https://zkcjskedmydfadscjgov.supabase.co"
SUPABASE_SERVICE_ROLE_KEY = "paste-the-service_role-key-here"
